# Speedy G env vars

# OpenAI key used by the /api/chat route that powers the floating chat bubble.
# Get one at https://platform.openai.com/api-keys
# Set this in Vercel: Project → Settings → Environment Variables.
OPENAI_API_KEY=sk-proj-...
